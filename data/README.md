# Pilot data

Put pairing exports, award reports and bid-screen screenshots from crew in `data/private/`. That
folder is gitignored; never commit its contents. Remove names and employee numbers before a file is
imported anywhere.
